#ifndef COLOUR_H
# define COLOUR_H
# include "datatypes.h"

unsigned int	fetch_colour1(unsigned int n, t_colours colour, \
	unsigned int max_iterations);
unsigned int	fetch_colour2(unsigned int n, t_colours colour, \
	unsigned int max_iterations);
unsigned int	fetch_colour3(unsigned int n, t_colours colour, \
	unsigned int max_iterations);

#endif
